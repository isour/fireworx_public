mixin breadcrumbs(data)
	+b.breadcrumbs&attributes(attributes)
		if(data && data.length)
			each item, index in data
				if (item.link)
					+e('a').link(href="#", title=item.title) #{item.title}
					+e.arrow
				else
					+e('span').text #{item.title}